import { SystemGallery } from "@/components/shell/system-gallery";

export const metadata = {
  title: "Design system - GV OS",
};

export default function SystemPage() {
  return <SystemGallery />;
}
